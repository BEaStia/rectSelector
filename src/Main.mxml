<?xml version="1.0"?>
<s:Application xmlns:fx="http://ns.adobe.com/mxml/2009" width="600" height="600" initialize="onCreate()" xmlns:s="library://ns.adobe.com/flex/spark"
               xmlns:mx="library://ns.adobe.com/flex/mx">
    <fx:Script>
        <![CDATA[
        import ru.sq.Field;
        import mx.core.UIComponent;

        import ru.sq.FieldWithEmbedded;

        private function onCreate():void {
            var ref:UIComponent = new UIComponent();

//            var child:FieldWithEmbedded = new FieldWithEmbedded();
            var child:Field = new Field();
            addElement(ref);
            ref.addChild(child);
        }
        ]]>
    </fx:Script>
</s:Application>
